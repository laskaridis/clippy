## Codebase Patterns
- Use `dev-sandbox` as the Dev Containers service name for the local sandbox, and publish the backend on the same `DJANGO_DEV_PORT` value inside the container and on the host.
- Keep `.devcontainer/.env.example` focused on the minimal localhost contract: `ALLOWED_HOSTS` should cover `localhost`, `127.0.0.1`, and `[::1]`, and per-worktree hostnames should not be documented there.
- Use the active container Python interpreter for bootstrap installs via `python -m pip`, and keep `.devcontainer/bootstrap.sh` as a thin wrapper that delegates setup to `make backend-init` and `make extension-init`.
- Prefer `$(MAKE)` subtarget composition for aggregate Makefile targets instead of shell wrapper scripts.
- Keep `make backend-run` as the explicit Django launch path inside the devcontainer: `cd backend && python manage.py migrate`, bootstrap the admin user with `apps.accounts.bootstrap.ensure_admin_user_from_env`, then run `python manage.py runserver 0.0.0.0:${DJANGO_DEV_PORT}`.
- Backend test wrappers should assume the devcontainer is already prepared and call `python manage.py test ...` directly from `backend/` without re-running runtime bootstrap helpers.
- Backend unit and E2E tests share the same database contract, so run them sequentially against one `DATABASE_URL` instead of in parallel when they point at the same Postgres instance.
- When deleting legacy helper scripts, first neutralize any shell tests that hardcode those filenames so repo-wide reference audits go green before the files are removed.
- Obsolete host-runtime tests can be deleted once their Make-target dependencies are gone; keep repo-wide `rg` audits focused on the code and script surfaces that still matter after the deletion.
- Rewrite `extension/chrome/runtime-config.js` in place from environment values using `WEBCLIPPINGS_API_BASE_URL` first and `DJANGO_DEV_PORT` as the localhost fallback; avoid creating `.local` extension artifacts for local development.
- Keep the extension shared config precedence fixed: runtime config first, the first valid `manifest.json` host permission second, and `http://localhost:8000` last.
---
## [2026-05-02 12:18Z]
- Task id
  - task-11
- What was implemented
  - Updated the extension manifest to allow localhost development across arbitrary backend ports via `http://localhost/*` and `http://127.0.0.1/*`.
  - Tightened the shared config tests to cover runtime override precedence, manifest-order fallback, invalid runtime override fallback, and the hard-coded localhost fallback.
  - Kept the shared config implementation precedence unchanged while updating the host-permission comment to match the new localhost wildcard contract.
- Files changed
  - `extension/chrome/manifest.json`
  - `extension/chrome/src/shared/config.ts`
  - `extension/chrome/src/shared/config.test.ts`
  - `extension/scripts/write-runtime-config.mjs`
  - `AGENTS.md`
  - `docs/plans/devcontainers/tasks.json`
  - `docs/plans/devcontainers/ralph.txt`
- **Learnings for future iterations:**
  - Patterns discovered: the extension config helper should always resolve API origins in three tiers, with manifest host permissions acting as the explicit fallback layer.
  - Gotchas encountered: `make extension-verify` also checks formatting, so an unrelated Prettier drift in `extension/scripts/write-runtime-config.mjs` had to be fixed before the task could be considered done.
  - Useful context: the manifest fallback now expects localhost wildcard permissions in order, which lets local development work across arbitrary backend ports without reintroducing worktree-specific hostnames.
---
## [2026-05-02 12:00Z]
- Task id
  - task-09b
- What was implemented
  - Deleted the obsolete `infra/local/scripts/tests/test_local_env_targets.sh` shell test now that the legacy local-env Make targets are gone.
- Files changed
  - `infra/local/scripts/tests/test_local_env_targets.sh`
  - `docs/plans/devcontainers/tasks.json`
  - `docs/plans/devcontainers/ralph.txt`
- **Learnings for future iterations:**
  - Patterns discovered: obsolete test files can be removed cleanly once their dependent Make targets disappear, without needing a replacement stub.
  - Gotchas encountered: the task-specific `rg` audit intentionally excludes `docs/plans/**`, so updating the task tracker itself does not affect the reference check.
  - Useful context: this deletion keeps the infra tree aligned with the new devcontainer-first workflow and clears the path for later gate/skill cleanups.
---
## [2026-05-02 08:40Z]
- Task id
  - task-07
- What was implemented
  - Simplified `backend/scripts/test-e2e.sh` so it no longer depends on legacy worktree runtime scripts or `.venv` paths.
  - Kept Chromium installation as an optional best-effort `python -m playwright install chromium` step and preserved the default E2E test target plus passthrough arguments.
- Files changed
  - `backend/scripts/test-e2e.sh`
  - `docs/plans/devcontainers/tasks.json`
  - `docs/plans/devcontainers/ralph.txt`
- **Learnings for future iterations:**
  - Patterns discovered: backend test wrappers can assume the devcontainer is already prepared and only need to manage per-command test behavior.
  - Gotchas encountered: the backend unit and E2E test targets both want to create the same PostgreSQL test database from one `DATABASE_URL`, so they must run sequentially in this environment.
  - Useful context: the `backend/scripts/test-e2e.sh --help` path is a quick sanity check that the wrapper no longer references removed runtime artifacts.
---
## [2026-05-02 08:49Z]
- Task id
  - task-09a
- What was implemented
  - Deleted the obsolete infra worktree runtime helper scripts: `infra/local/scripts/manage-worktree-compose.sh` and `infra/local/scripts/ensure-worktree-runtime.sh`.
  - Reworked `infra/local/scripts/tests/test_local_env_targets.sh` into a current Makefile-help smoke test that no longer hardcodes the removed helper script paths.
- Files changed
  - `infra/local/scripts/manage-worktree-compose.sh`
  - `infra/local/scripts/ensure-worktree-runtime.sh`
  - `infra/local/scripts/tests/test_local_env_targets.sh`
  - `docs/plans/devcontainers/tasks.json`
  - `docs/plans/devcontainers/ralph.txt`
- **Learnings for future iterations:**
  - Patterns discovered: script-removal tasks may need a companion test rewrite before repo-wide `rg` checks can pass, because shell smoke tests often hardcode the filenames being deleted.
  - Gotchas encountered: the `infra/local/scripts/tests/test_local_env_targets.sh` guard was still pointing at the deleted helper scripts, so the removal would not have been verifiable until that test was neutralized.
  - Useful context: the updated smoke test now only checks `make help` for the absence of `local-env-` targets and the presence of `backend-run`, which keeps a useful guard in place until the later test deletion task runs.
---
## [2026-05-02 08:37Z]
- Task id
  - task-06
- What was implemented
  - Simplified `backend/scripts/test.sh` to drop the worktree-runtime bootstrap and run `python manage.py test --exclude-tag=e2e` directly inside `backend/`.
- Files changed
  - `backend/scripts/test.sh`
  - `docs/plans/devcontainers/tasks.json`
  - `docs/plans/devcontainers/ralph.txt`
- **Learnings for future iterations:**
  - Patterns discovered: backend test wrappers should be thin devcontainer-local shims that forward arguments directly to `manage.py test`.
  - Gotchas encountered: the backend test target still needs a valid `DATABASE_URL`; outside the devcontainer I had to point it at the already-running Postgres container to get the verification pass.
  - Useful context: `backend/scripts/test.sh --help` remains a useful smoke check because it now reflects the prepared-devcontainer contract without needing database access.
---
## [2026-05-02 08:33Z]
- Task id
  - task-04
- What was implemented
  - Removed the obsolete runtime-management entry from `Makefile`'s `.PHONY` list.
  - Switched `all-test` to compose `backend-test-unit` and `backend-test-e2e` with `$(MAKE)` directly.
  - Trimmed the remaining target comments so they no longer describe worktree-specific runtime artifacts.
- Files changed
  - `Makefile`
  - `docs/plans/devcontainers/tasks.json`
  - `docs/plans/devcontainers/ralph.txt`
- **Learnings for future iterations:**
  - Patterns discovered: aggregate project targets should invoke other Make targets directly with `$(MAKE)` so the command surface stays explicit.
  - Gotchas encountered: `make all-test` still reaches the legacy backend E2E script, which currently expects later cleanup tasks and therefore fails on the old `.venv` path.
  - Useful context: the top-level unit-test path is already healthy, so the remaining `all-test` failure is downstream of this task rather than caused by the Makefile refactor.
---
## [2026-05-02 11:31Z]
- Task id
  - task-03
- What was implemented
  - Reworked `backend-init` to install backend dev requirements with the active container Python instead of creating `backend/.venv`.
  - Updated `make help` so backend bootstrap and backend run descriptions match the container-first workflow.
  - Replaced raw install commands in `.devcontainer/bootstrap.sh` with `make backend-init` and `make extension-init`.
- Files changed
  - `Makefile`
  - `.devcontainer/bootstrap.sh`
- **Learnings for future iterations:**
  - Patterns discovered: devcontainer bootstrap should delegate to repository Make targets from `/workspace` instead of duplicating package-manager commands.
  - Gotchas encountered: `make backend-init` now runs against the active Python environment, so later tasks should avoid reintroducing `backend/.venv` assumptions anywhere in the command surface.
  - Useful context: the bootstrap script now relies on `make` being available inside the devcontainer image, which is consistent with the container-first setup in this plan.
---
## [2026-05-02 08:29Z]
- Task id
  - task-02
- What was implemented
  - Simplified `.devcontainer/.env.example` to the localhost-plus-port contract by removing `DJANGO_DEV_HOST` and `DJANGO_DEV_BASE_URL` and narrowing `ALLOWED_HOSTS`.
- Files changed
  - `.devcontainer/.env.example`
- **Learnings for future iterations:**
  - Patterns discovered: the tracked devcontainer env example should only document the minimal localhost contract needed for browser access.
  - Gotchas encountered: the example file still had the old `clippy.localhost` hostname, so future edits should strip any remaining worktree-specific origin values.
  - Useful context: `POSTGRES_HOST=postgres` and `DATABASE_URL` already encode the compose-network database contract, so only the frontend-facing host settings needed trimming.
---
## [2026-05-02 00:00Z]
- Task id
  - task-01
- What was implemented
  - Renamed the devcontainer service from `app` to `dev-sandbox`.
  - Removed the legacy `DJANGO_DEV_HOST` and `DJANGO_DEV_BASE_URL` passthroughs.
  - Removed the PostgreSQL host port binding and added a backend port mapping driven by `DJANGO_DEV_PORT`.
- Files changed
  - `.devcontainer/docker-compose.yml`
  - `.devcontainer/devcontainer.json`
- **Learnings for future iterations:**
  - Patterns discovered: the devcontainer service name is now `dev-sandbox`, and host-visible backend access should be keyed off `DJANGO_DEV_PORT`.
  - Gotchas encountered: PostgreSQL should stay internal to the compose network; only the backend needs host exposure for browser verification.
  - Useful context: `devcontainer.json` still points at `.devcontainer/bootstrap.sh`, so later tasks can update bootstrap behavior without reworking the service name again.
---
## [2026-05-02 11:50Z]
- Task id
  - task-05
- What was implemented
  - Redefined `make backend-run` to run migrations, ensure the local admin user exists, and start Django directly inside `backend/` on `0.0.0.0:${DJANGO_DEV_PORT}`.
- Files changed
  - `Makefile`
  - `docs/plans/devcontainers/tasks.json`
  - `docs/plans/devcontainers/ralph.txt`
- **Learnings for future iterations:**
  - Patterns discovered: backend startup commands should stay explicit in the Makefile so the devcontainer workflow does not depend on hidden wrapper scripts.
  - Gotchas encountered: the shell needs `$$` in the Makefile recipe so `DJANGO_DEV_PORT` is expanded at execution time rather than by `make`.
  - Useful context: `make backend-test-unit` still passes after the recipe change, which gives a clean baseline before the later backend script deletions.
---
## [2026-05-02 08:47Z]
- Task id
  - task-08a
- What was implemented
  - Deleted the legacy backend lifecycle scripts: `backend/scripts/start-server.sh`, `backend/scripts/stop-server.sh`, and `backend/scripts/check-server.sh`.
  - Removed the remaining live reference from `scripts/run_worktree_stack.sh` so it now calls `make backend-run` directly and rejects obsolete passthrough arguments.
  - Trimmed `backend/README.md` to describe the devcontainer-local backend start flow instead of the removed lifecycle wrappers.
- Files changed
  - `backend/README.md`
  - `backend/scripts/check-server.sh`
  - `backend/scripts/start-server.sh`
  - `backend/scripts/stop-server.sh`
  - `docs/plans/devcontainers/tasks.json`
  - `docs/plans/devcontainers/ralph.txt`
  - `scripts/run_worktree_stack.sh`
- **Learnings for future iterations:**
  - Patterns discovered: the remaining wrapper scripts should fail loudly once their direct backend entrypoints disappear rather than forwarding arguments into `make` unexpectedly.
  - Gotchas encountered: repository verification depends on a live PostgreSQL database named `webclippings`; the local container had the role but not the database, so I had to create it before rerunning the suite.
  - Useful context: `make all-verify` exercises the extension accessibility audit, which currently reports a serious color-contrast finding on the login page even though the backend and extension unit/e2e suites pass.
---
## [2026-05-02 08:52Z]
- Task id
  - task-10
- What was implemented
  - Added `extension/scripts/write-runtime-config.mjs` to rewrite `extension/chrome/runtime-config.js` in place from `WEBCLIPPINGS_API_BASE_URL` or `DJANGO_DEV_PORT` without creating worktree-specific `.local` artifacts.
  - Rewired `extension/package.json` to remove the worktree-specific build scripts and run the runtime-config generator before Playwright e2e and a11y tests.
- Files changed
  - `extension/package.json`
  - `extension/scripts/write-runtime-config.mjs`
  - `docs/plans/devcontainers/tasks.json`
  - `docs/plans/devcontainers/ralph.txt`
- **Learnings for future iterations:**
  - Patterns discovered: the extension can keep a tiny checked-in runtime config file as long as the local generator rewrites it in place from environment values.
  - Gotchas encountered: passing `--list` through `pnpm run ... -- --list` forwards the separator literally to Playwright, so direct `pnpm exec playwright test ... --list` is the cleaner smoke check for script wiring.
  - Useful context: `prepare:runtime-config` runs before both `test:e2e` and `test:a11y`, so any future extension Playwright helper should assume `runtime-config.js` already reflects the active devcontainer.
---
