## Codebase Patterns
- When multiple editable installs expose the same top-level package, verify `sys.meta_path` ordering; distribution naming can affect which checkout wins.
- Use frozen dataclasses for run-context and terminal outcome types so orchestration state stays immutable and easy to reason about.
- Keep built-in registries static and expose a dedicated lookup helper that raises a specific unknown-item error so callers can fail fast without inspecting the mapping directly.
- Load bundled prompt assets with `importlib.resources.files("ralph")` so prompt lookups work the same in editable installs and packaged wheels.
- Keep the orchestrator's step implementation imports local inside the run loop when the workflow module imports orchestrator types, so the package avoids circular import failures.

## [2026-05-22 11:59:04 EEST]
- Task id
  - TASK-001
- What was implemented:
  - Added a root `pyproject.toml` so the repository installs as an editable package and exposes a `ralph` console script.
  - Added the initial `ralph` Python package with `__init__.py`, `__main__.py`, and a minimal `cli.py` that parses the `run` subcommand.
- Files changed:
  - `pyproject.toml`
  - `ralph/__init__.py`
  - `ralph/__main__.py`
  - `ralph/cli.py`
- Verification:
  - `python -m pip install -e .` completed successfully.
  - `python -m compileall ralph` completed successfully.
  - `ralph --help` printed the new CLI help showing the `run` subcommand.
  - `python -m ralph --help` printed the same CLI help.
- Learnings for future iterations:
  - Editable installs from other worktrees can shadow this checkout when top-level package names collide; checking `sys.meta_path` helped confirm the active finder order.
  - For this repo, the console script should be validated both via `python -m ralph` and the installed `ralph` executable because the shell may already have an unrelated command cached.
- Scope deviations:
  - None.
- Follow up needed:
  - None.
---
## [2026-05-22 12:04:11 EEST]
- Task id
  - TASK-002
- What was implemented:
  - Added immutable shared lifecycle contracts in `ralph/lifecycle.py` for the four allowed step outcomes: `repeat`, `complete`, `blocked`, and `fail`.
  - Added orchestrator-facing run-context and terminal outcome types in `ralph/orchestrator.py`, including a stable `run_id` and nullable `log_path` metadata.
- Files changed:
  - `ralph/lifecycle.py`
  - `ralph/orchestrator.py`
  - `docs/plans/ralph-harness/tasks.json`
  - `docs/plans/ralph-harness/ralph.txt`
- Verification:
  - `python -m compileall ralph` completed successfully.
  - `python -m py_compile ralph/lifecycle.py ralph/orchestrator.py` completed successfully.
  - A direct import probe instantiated `RunContext` and each step/run outcome type and confirmed the expected defaults and literal outcome values.
- Learnings for future iterations:
  - Frozen dataclasses plus `Literal`-typed outcome fields give a compact contract layer that is easy to extend without introducing hidden mutation.
  - Keeping the run identifier on the immutable run context makes later logging and orchestration code simpler because all shared run metadata travels together.
- Scope deviations:
  - None.
- Follow up needed:
  - None.
---
## [2026-05-22 12:09:37 EEST]
- Task id
  - TASK-003
- What was implemented:
  - Added the `ralph.agents` package with a typed `AgentRequest`/`AgentResult` contract and a protocol for built-in agent implementations.
  - Added a static built-in registry that resolves `codex` and raises `UnknownAgentError` for unknown names.
  - Implemented `CodexAgent` as a single AFK subprocess invocation that preserves the Codex raw response file content and process metadata.
- Files changed:
  - `ralph/agents/__init__.py`
  - `ralph/agents/base.py`
  - `ralph/agents/codex.py`
  - `docs/plans/ralph-harness/tasks.json`
  - `docs/plans/ralph-harness/ralph.txt`
- Verification:
  - `python -m compileall ralph` completed successfully.
  - A direct import probe resolved `codex`, rejected an unknown agent with `UnknownAgentError`, and instantiated `AgentRequest` with the expected typed fields.
- Learnings for future iterations:
- Static built-in registries are a clean fit for Ralph because the spec wants fast failure for unknown names and no runtime discovery.
- The Codex adapter should capture the output file before cleanup so later steps can parse the raw agent response reliably.
- Scope deviations:
  - None.
- Follow up needed:
  - None.
---
## [2026-05-22 12:48:00 EEST]
- Task id
  - TASK-004
- What was implemented:
  - Added `ralph.workflow.CodeStep` as a single-shot step that loads the bundled code prompt, invokes the selected agent once, and converts strict JSON responses into typed step results.
  - Updated `ralph/prompts/code.md` so the built-in coding prompt now requires JSON-only output with explicit outcome-specific fields.
- Files changed:
  - `ralph/workflow/__init__.py`
  - `ralph/workflow/code.py`
  - `ralph/prompts/code.md`
  - `docs/plans/ralph-harness/tasks.json`
  - `docs/plans/ralph-harness/ralph.txt`
- Verification:
  - `python -m compileall ralph` completed successfully.
  - A direct smoke script instantiated `CodeStep` with a fake agent and verified `complete`, `blocked`, and malformed-JSON responses map to `CompleteStepResult`, `BlockedStepResult`, and `FailedStepResult` respectively.
  - The smoke script also verified the rendered prompt includes the selected feature directory path and that malformed JSON preserves the raw response.
- Learnings for future iterations:
  - Bundled prompt assets are safer to load with `importlib.resources.files("ralph")` than with filesystem-relative paths, because the same code then works in editable installs and packaged wheels.
  - The Code step should keep strict JSON validation local so future orchestrator work can rely on typed step results instead of ad hoc string parsing.
- Scope deviations:
  - None.
- Follow up needed:
  - None.
---
## [2026-05-22 12:19:47 EEST]
- Task id
  - TASK-005
- What was implemented:
  - Added feature-directory validation plus a hard-coded run loop in `ralph/orchestrator.py` that executes the built-in `CodeStep`, handles `repeat` / `complete` / `blocked` / `fail`, and produces deterministic terminal run outcomes.
  - Expanded `ralph/cli.py` to parse `run` options for agent, model, max iterations, and optional log path, then map typed run outcomes to stable exit codes and terminal messages.
- Files changed:
  - `ralph/orchestrator.py`
  - `ralph/cli.py`
  - `docs/plans/ralph-harness/tasks.json`
  - `docs/plans/ralph-harness/ralph.txt`
- Verification:
  - `python -m compileall ralph` completed successfully.
  - `python -m ralph run --help` showed the `run` subcommand plus `--agent`, `--model`, `--max-iterations`, and `--log`.
  - `python -m ralph run /tmp/ralph-harness-does-not-exist-12345` exited with code `2` and reported the missing feature directory before any agent work started.
  - `python -m ralph run /tmp/ralph-harness-does-not-exist-12345 --max-iterations 0` exited with code `2` and rejected the invalid iteration limit at argument parsing time.
- Learnings for future iterations:
  - The orchestrator should import the workflow step lazily inside the run loop to avoid a circular import between `ralph.orchestrator` and `ralph.workflow.code`.
  - `argparse` is a clean fit for these CLI defaults because invalid numeric input can fail fast before the run context is even created.
- Scope deviations:
  - None.
- Follow up needed:
  - None.
---
