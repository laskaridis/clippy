# Required host secrets
# `make sandbox-start` forwards only these required values into the sandbox.
OPENAI_API_KEY=
GH_TOKEN=

# Optional OpenAI settings
OPENAI_BASE_URL=
OPENAI_ORG_ID=
OPENAI_PROJECT_ID=
